---
- name: daemon reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart mariadb_exporter
  ansible.builtin.service:
    name: "{{ mariadb_exporter_service_name }}"
    state: restarted
